#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

/*
* 접근법
* MenOfPassion()의 역할은 배열의 중간 값을 리턴하는 것이다.
* 그렇기 때문에 코드의 수행 횟수는 언제나 1이 된다.
* 따라서 해당 수행횟수를 다항식으로 표현하면
* 최고차항이 0인 상수항이 된다.
* 결론 : 출력은 언제나 1 과 0이 된다.
*/

int MenOfPassion(int A[], int n) {
    int i = (n / 2);
    return A[i];
}

int main() {
    int count; scanf("%d", &count);
    printf("1\n0");

	return 0;
}
